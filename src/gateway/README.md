# OpenBrisk API Gateway

The API gateway provides access to the functions deployed on the Kubernetes cluster.

## Features

- Invoke deployed functions
- Autoscaled using Kubernetes Deployment
- Accessible via REST API
